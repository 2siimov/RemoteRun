package bin.debug;

public class FastCommand2 {
    public static void main(String[] args) throws Exception {
        /*HostSend hs = new HostSend(CommandlineUI.getSocketConfig());
        hs.getStream();
        for (;;) {
            System.out.printf("Send: ");
            String cmd = new Scanner(System.in).nextLine();
            hs.sendCmd(cmd);
        }*/
    }
}
